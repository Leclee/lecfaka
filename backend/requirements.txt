# FastAPI 核心
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# 数据库
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0  # PostgreSQL异步驱动
aiomysql>=0.2.0  # MySQL异步驱动(可选)
alembic>=1.13.1

# 缓存（redis>=4.2 已内置 redis.asyncio，不再需要 aioredis）
redis>=5.0.1

# 认证
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# 数据验证
pydantic>=2.5.3
pydantic-settings>=2.1.0
email-validator>=2.1.0

# HTTP客户端
httpx>=0.26.0
aiohttp>=3.9.1

# 工具
python-dotenv>=1.0.0
Pillow>=10.2.0
qrcode>=7.4.2

# 加密
cryptography>=41.0.7

# 邮件
aiosmtplib>=3.0.1

# 开发测试
pytest>=7.4.4
pytest-asyncio>=0.23.3

# 日志
loguru>=0.7.2
