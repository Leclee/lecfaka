# ========================================
# LecFaka 环境变量配置
# 复制为 .env 后修改
# ========================================

# ---------- 安全密钥（务必修改！）----------
# 生成方法: openssl rand -hex 32
SECRET_KEY=请替换为随机字符串
JWT_SECRET_KEY=请替换为另一个随机字符串

# ---------- 数据库 ----------
DB_USER=lecfaka
DB_PASSWORD=请替换为强密码
DB_NAME=lecfaka

# ---------- 默认管理员（首次启动自动创建）----------
ADMIN_USERNAME=admin
ADMIN_PASSWORD=请替换为强密码
ADMIN_EMAIL=admin@example.com

# ---------- 端口配置（可选）----------
# HTTP_PORT=80              # 前端端口，默认 80
# DB_PORT=127.0.0.1:5432    # 数据库端口，默认仅本机
# REDIS_PORT=127.0.0.1:6379 # Redis 端口，默认仅本机

# ---------- 插件商店（可选）----------
# 默认连接官方商店，无需修改
# 如需连接自建商店，取消注释并填写地址：
# STORE_URL=https://store.example.com

# ---------- 站点配置（可选）----------
# 生产环境不需要设置，由 Nginx 反向代理自动检测
# 本地开发时如需覆盖可取消注释：
# SITE_URL=http://localhost:5173
# CALLBACK_URL=http://localhost:6666

# ---------- 注意 ----------
# 支付方式（易支付、USDT 等）均在管理后台网页中配置
# 不需要在此文件中设置任何支付参数
