# ========================================
# LecFaka 环境变量配置
# 复制为 .env 后修改（backend/.env 用于本地开发）
# ========================================

# 应用配置
APP_NAME=LecFaka
APP_ENV=production
DEBUG=false

# 安全密钥（务必修改为随机字符串！）
# SECRET_KEY: 用于加密 session、CSRF 等安全数据
# JWT_SECRET_KEY: 用于签名和验证用户登录 token
SECRET_KEY=请替换为随机字符串
JWT_SECRET_KEY=请替换为另一个随机字符串

# 数据库配置
# 本地开发: postgresql+asyncpg://lecfaka:lecfaka123@localhost:5432/lecfaka
# Docker:   由 docker-compose 自动配置，无需修改
DATABASE_URL=postgresql+asyncpg://lecfaka:lecfaka123@localhost:5432/lecfaka
DB_USER=lecfaka
DB_PASSWORD=lecfaka123
DB_NAME=lecfaka

# Redis 配置
REDIS_URL=redis://localhost:6379/0

# JWT 配置（可选，有默认值）
# JWT_ALGORITHM=HS256
# JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
# JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# 默认管理员账号（首次启动自动创建，之后可删除）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@lecfaka.com

# 站点配置（可选，留空自动从请求头检测）
# 生产环境不需要设置，由 Nginx 反向代理自动处理
# 本地开发时如需覆盖可取消注释：
# SITE_URL=http://localhost:8888
# CALLBACK_URL=http://localhost:6666

# ========================================
# 以下配置均在管理后台网页中设置，无需在此配置：
# - 易支付（管理后台 > 支付配置）
# - USDT 支付（管理后台 > 支付配置）
# - 邮件 SMTP（管理后台 > 系统设置 > 邮箱设置）
# - 短信（管理后台 > 系统设置 > 短信设置）
# ========================================
