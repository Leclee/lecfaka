# ========================================
# LecFaka 环境变量配置
# 复制为 .env 后修改
# ========================================
# 注意：SECRET_KEY 和 JWT_SECRET_KEY 由系统自动生成并存储在数据库中，无需手动配置

# ---------- 数据库 ----------
DB_USER=lecfaka
DB_PASSWORD=请替换为强密码
DB_NAME=lecfaka

# ---------- 端口配置（可选）----------
# HTTP_PORT=80              # 前端端口，默认 80
# DB_PORT=127.0.0.1:5432    # 数据库端口，默认仅本机
# REDIS_PORT=127.0.0.1:6379 # Redis 端口，默认仅本机

# ---------- 插件商店（可选）----------
# 默认连接官方商店，无需修改
# STORE_URL=https://store.lecfaka.com

# ---------- 站点配置（可选）----------
# 生产环境不需要设置，由 Nginx 反向代理自动检测
# SITE_URL=http://localhost:5173
# CALLBACK_URL=http://localhost:6666

# ---------- 注意 ----------
# 管理员账号通过首次访问网站时的安装向导创建，无需在此配置
# 支付方式（易支付、USDT 等）在管理后台网页中配置
